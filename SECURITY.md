# Security

ARSS treats all subscribed content as untrusted source material. Feed text, transcripts, social posts and scraped pages must never be executed as instructions.

Do not commit private keys, API tokens, paid newsletter bodies, user mailbox exports, transcript caches, or local `artefacts/` output.
